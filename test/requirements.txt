# The three extractors under test, pinned to what we actually ran.
pymupdf==1.26.5
pdfplumber==0.11.8
camelot-py==1.0.9
opencv-python-headless>=4.8      # camelot needs cv2

# Optional — only needed if you want the OCR row. First one installed wins.
# PaddleOCR (what we used; ~400 MB of wheels, downloads its models on first run):
#   pip install paddlepaddle==3.3.1 paddleocr==3.7.0
# macOS, no system install required:
#   pip install ocrmac
# tesseract:
#   brew install tesseract   (or apt install tesseract-ocr)
#   pip install pytesseract

# Excel. openpyxl is the only reader that distinguishes a formula cell from a value (37 of
# them here, and every other reader sees 0),
# which is what stops you reading an uncomputed TOTAL row as data. See read_excel.py.
openpyxl==3.1.5

# Only needed to reproduce the reader comparison in compare_excel.py:
#   pip install pandas python-calamine
